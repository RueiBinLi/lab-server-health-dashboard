# Keep the Dashboard on a Private Network

Deploy the dashboard on the lab LAN first and add Tailscale for remote access after the application is stable. The dashboard must not be exposed directly to the public internet; this reduces exposure of infrastructure details while still allowing authorized remote monitoring through a private network.
